{
  "name": "monorepo-demo",
  "version": "1.0.0",
  "private": true,
  "description": "pnpm Monorepo 演示项目",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "clean": "pnpm -r exec rm -rf node_modules dist"
  },
  "packageManager": "pnpm@8.10.0",
  "engines": {
    "node": ">=16.0.0"
  }
}

